
// This component has been replaced by ProfilePage.tsx
// Keeping this file for backward compatibility
import ProfilePage from '../ProfilePage';

const ProfileInfo = () => {
  return <ProfilePage />;
};

export default ProfileInfo;
